# splearn 도메인 모델

## 도메인 모델 만들기

1. 듣고 배우기
2. 중요한 것들 찾기 (개념 식별)
3. 연결고리 찾기 (관계 정의)
4. 것들을 설명하기 (속성 및 기본 행위 명시)
5. 그려보기 (시각화)
6. 이야기 하고 다듬기 (반복)

## `splaern` 도메인

### 회원(Member)

#### 속성
- 이메일
- 이름
- 닉네임
- 비밀번호
- 회원 상태
- `detail` : `MemberDetail` 

#### 행위
- `static register()` 회원 등록
- `activate()` 등록을 완료 시킨다.
- `deactivate()` 탈퇴 시킨다.
- `verifyPassword()` 비밀번호 검사

#### 규칙
- 회원 등록 후 상태는 등록 대기
- 일정조건 만족 후에 등록 완료 된다.
-  완료 상태에서는 탈퇴가 가능하다.
- 비밀번호 암호화, 검증

### 회원 상세(MemberDetail)
- `id`
- `profile`
- `introduction`
- `registeredAt`
- `activatedAt`
- `deactivatedAt`

### 회원 상태(enum)
- `pending` 대기
- `active` 활성
- `deactivated` 탈퇴

### 비밀 번호 인코더
_Domain Service_
도메인에 넣기에는 기술적이고 그런 것들을 인터페이스로 만든?
무언가?

#### 행위
- `encode()` : 비밀 번호 암호화
- `matches()` : 비밀번호 일치하는지 확인


### 강사

### 강의

### 수업

### 섹션

### 수강

### 진도

# 용어 사전

| 한국어 | 영어 | 설정  |  
|-----|----|-----|
| 회원  |    |     |   
| 강사  |    |     |
| 강의  |    | --- |
|     |    |     |
|     |    |     |
|     |    |     |
|     |    |     |
|     |    |     |
|     |    |     |


